# Hugo default
public
resources
.hugo_build.lock

# Mac system files
.DS_Store

# Docker/Dev container logs (optional)
*var/log*

# Claude Code - Ignore most of .claude/ directory
# Keep only: CLAUDE.md (project instructions) and .claude/settings.json (shared settings)
.claude/*
!.claude/settings.json

# Root-level Claude config (if present)
.claude.json
